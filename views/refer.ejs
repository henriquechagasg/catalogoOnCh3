<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/1c2c2462bf.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" type="text/css" href="/css/refer.css">
    <title>Document</title>
</head>
<body>

  
    <!-- NAvbar -->
    <div class="navbar">
      <div>
          <a onclick="back()" class="arrow-left">
            <i class="fas fa-arrow-left"></i>
          </a>
      </div>
      <h1 class="refer"><%= products[0].REFER.trim() %></h1>   
      </div>


      <% if (orders.length > 0) { %> 
      <div class="order-alert-area">
        <div class="alert">
          <h2><i class="fas fa-exclamation-triangle"></i>  Existe pedido pra essa referência.</h2>
          <button onclick="showModal()" id="orders-button">Visualizar</button>
        </div>
          <!-- <h1>Tem pedido da <%= products[0].DESCRI  %> </h1> -->
      </div>
      <div class="modal-container" id="orders-modal">
        <div class="modal">
          <button class="closeOrders-button" onclick="closeModal()"><i class="fas fa-times"></i></button>
          <% for(order of orders) { %> 
          <div class="order-line">
            <p><%= order.DESCR %> </p>
            <div class="ordered-quantitys">
              <% if (order.P) { %> 
              <p><b><%= order.P %> </b> P</p>
              <% } %>
              <% if (order.M) { %> 
              <p><b><%= order.M %> </b> M</p>
              <% } %>
              <% if (order.G) { %> 
              <p><b><%= order.G %> </b> G</p>
              <% } %>
              <% if (order.GG) { %> 
              <p><b><%= order.GG %> </b> GG</p>
              <% } %>     
            </div>          
          </div>
          <% } %> 
     
          
        </div>
      </div>
      <% } %> 

      
  

  <!-- Card para imagens: -->
    <div class="container">
      <% for (product of products) { %> 
      <% product.DESCR = product.DESCR.replace(regex, "-")
         product.REFER = product.REFER.replace(regex, '-') %>  
      <div class="card" id="product-card">
        <div class="card-image">
          <img src="/imgs/<%=product.REFER.trim()%>&<%=product.DESCR.trim()%>.jpg" alt="Este item nao tem foto =/">
        </div>
          <div class="card-text">
            <p><b><%= product.DESCRI.trim() %></b></p>
            <p> <%= product.DESCR.trim() %> </p>
            <section class="quantity-cards">
              <% if (product.P) { %> 
              <div class="size-cards">
                <p><%=product.P%></p>
                <b>P</b> 
              </div>
              <% } %>
              <% if (product.M) { %> 
              <div class="size-cards">
                <p><%=product.M%></p>
                <b>M</b> 
              </div>
              <% } %>
              <% if (product.G) { %> 
              <div class="size-cards">
                <p><%=product.G%></p>
                <b>G</b> 
              </div>
              <% } %>
              <% if (product.GG) { %> 
              <div class="size-cards">
                <p><%=product.GG%></p>
                <b>GG</b> 
              </div>
              <% } %>  
              

            </section>
              
          </div>
        </div>
        <% } %>
      </div>
    
  </div>
 

    <script>
      const modal = document.querySelector("#orders-modal")
      function back () {
          window.history.back()

      }
      function showModal () {
        modal.classList.add("show")
      }
      function closeModal () {
        modal.classList.remove("show")
      }
    </script>
    <script src="/js/main.js"></script>
</body>
</html>